import MainMenu from '../routes/MainMenu';
import Curation from '../routes/Curation';

const Layout = () => {
  return (
    <div>
      <MainMenu />
      <Curation />
    </div>
  );
};

export default Layout;
